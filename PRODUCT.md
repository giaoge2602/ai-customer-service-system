# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- **客户/访客（本组件的服务对象）**：星河科技（演示租户）的潜在或现有客户，在官网/H5/小程序内嵌页面遇到售前或售后问题（退款、部署、密码等），不想注册账号，希望立刻得到答案或转人工。场景多为手机或桌面浏览器右下角随手打开。
- **客服坐席**：在客服工作台接待从该窗口升级上来的人工会话（不直接使用本组件）。

## Product Purpose

多租户 AI 智能客服系统的**免登录客户聊天窗口**（CustomerChatWidget）。任意页面通过 `tenantId + channel` 挂载右下角客服气泡，客户免注册、打开即聊：AI 基于企业知识库接待，工作时段可升级人工，支持图片/文件/语音多媒体消息、断线重连、服务评价。成功 = 客户在最短时间内得到可靠回答，或顺畅转人工。

## Positioning

一套代码同时服务「AI 秒答 + 人工兜底」闭环：访客 token 换取、会话持久化（同一浏览器回来接着聊）、实时事件（排队/接管/结束/评价）全部封装在 `chatClient.js` SDK 内，嵌入方只需要一行挂载。竞品演示页做不到的：多媒体消息（鉴权 blob、语音转写）与可审计的 AI→人工交接在同一个小窗口里闭环。

## Operating Context

- 演示/评审场景为主：后端未启动时自动降级本地演示数据；评审者会打开窗口测试各状态（连接中、排队、错误 502、评价）。
- 嵌入环境不可控：宿主页面可能是亮色营销页或深色后台，组件必须样式隔离（`csw-` 前缀、独立 CSS 文件，不依赖页面栅格）。
- 与门户登录态完全隔离（localStorage 独立 key）。

## Capabilities and Constraints

- 能力：快捷问题、文本消息、图片/文件/语音上传（进度与阶段提示）、语音录制状态机（idle/recording/preview）、多媒体消息渲染（lightbox、文件卡片、语音波形+转写）、服务评价卡、会话结束感谢卡。
- 状态面：加载中、未建会话（欢迎态）、实时已连接/正在连接、排队中、人工接待中、已结束、已评价（只读）、错误（如请求失败 502）、上传中、录音中。
- 约束：React 19 + Vite 7；主题色经 `theme` prop 注入（`--csw-theme`，默认 `#315bea`）；文案中文；面板宽约 360px、悬浮于宿主页面右下角；移动端 ≤480px 需收窄。
- 未决：是否展示「消息云端保存」类承诺文案（用户已同意改为客户视角措辞）。

## Brand Commitments

- 名称「星河客户服务」，头像字符「星」。
- 主题色 `#315bea` 为默认值（可被嵌入方覆盖，组件不得写死为唯一样式来源）。
- 中文界面文案；语气友好、面向客户，不暴露内部术语。

## Evidence on Hand

- README.md（产品全景与功能清单）、docs/client-integration.md（接入文档）。
- 现有实现：src/chat/*（组件、SDK、多媒体、录音）。
- 无真实客户数据/截图素材；演示租户「星河科技」为虚构。

## Product Principles

1. **打开即聊**：不注册、不填表单，第一屏就给出可点的路（快捷问题或输入框）。
2. **状态透明**：连接、排队、失败、只读，每个状态都用客户能懂的话说清楚，并给出下一步。
3. **样式隔离是硬约束**：宿主页面再乱，窗口自身视觉完整。
4. **多媒体不打断对话流**：图片/文件/语音像文本一样自然出现在气泡流里。
5. **错误可恢复**：网络失败要告诉客户发生了什么、怎么重试，而不是一行状态码。

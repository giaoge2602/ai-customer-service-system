# 前端改进落地说明（按 frontend-improvements.md）

> 日期：2026-08-25 ｜ 依据：`docs/frontend-improvements.md` ｜ 范围：纯前端，不涉及后端联调

## 落地清单（13/13）

### 🔴 P0 — 核心使用
| # | 改进 | 落地点 |
| --- | --- | --- |
| 1 | 看板数据"活"模拟 | `ServiceWorkspace.jsx` TeamDashboard：`setInterval` 每秒 SLA 递减（`slaLeft` map）、排队数 `queueFlux` 波动、客服状态概率切换，附 `/* TODO: 替换为后端 /api/v1/dashboard/realtime */` |
| 2 | 排队排序 + 一键接入 | `App.jsx`：`filtered` 按 SLA 风险排序（danger→warning→safe）；列头加「一键接下一」按钮 `claimNext`，自动接入最高优先级待处理会话 |
| 3 | 拆分 AdminConsole | 渐进式：抽出 `src/components/AgentList.jsx`（客服列表+批量操作，平台/机构复用）；`PlatformOverview/OrgOverview/KnowledgeList` 后续跟进 |

### 🟡 P1 — 体验优化
| # | 改进 | 落地点 |
| --- | --- | --- |
| 4 | SLA 倒计时组件化 | 新建 `src/SlaCountdown.jsx`（秒数倒计时 + `slaRiskByLeft` 动态着色 + `parseSlaToSeconds`）；看板队列卡片与 SLA 面板接入，数字每秒跳动 |
| 5 | AI 辅助按钮移工具栏 | `App.jsx`：`ai-trigger` 从消息流底部移到 `composer-toolbar`（`ai-trigger-tool`，与快捷回复/附件并列） |
| 6 | 评价组件增强 | `CustomerChat`：加 `rating-text` 文字框 + 提交后 `rating-thanks` 渐变感谢卡片（`slideUp` 动画，2 秒后转为静态"已提交"） |
| 7 | 筛选写入 URL | `App.jsx`：`useSearchParams`（q/status/channel/risk），`updateParam` 用 `replace` 模式，刷新/分享不丢筛选 |
| 8 | 表格批量操作 | `AgentList`：表头全选（indeterminate 态）+ 行选 + 底部 `bulk-bar`（批量启用/停用/导出，附 `TODO 批量 API`） |

### 🟢 P2 — 细节打磨
| # | 改进 | 落地点 |
| --- | --- | --- |
| 9 | 客服状态切换入口 | `AgentWorkspaceShell` topbar：`presence-select` 下拉（在线接待 / 忙碌不分配 / 离线），点击外部关闭 |
| 10 | 右侧面板默认关闭 | `App.jsx`：`rightOpen` 默认 `false`；接管摘要折叠进 `HandoffBanner`（`<details>`） |
| 11 | 邀请码引导 | `AuthPage.jsx`：注册表单加试用账号引导（`admin@ai-service.demo` / `Demo@2026`，可一键复制） |
| 12 | 移动端手势关闭 | `AgentWorkspaceShell`：`mobile-rail-drawer` 监听 `touchstart/touchend`，左滑 >80px 关闭导航 |
| 13 | 空状态统一 | 新建 `src/EmptyState.jsx`；App.jsx 会话空列表、AgentList 接入，视觉一致 |

## 新增/改动文件

| 文件 | 类型 |
| --- | --- |
| `src/SlaCountdown.jsx` | 新建 |
| `src/EmptyState.jsx` | 新建 |
| `src/components/AgentList.jsx` | 新建（AdminConsole 拆分第一步） |
| `src/App.jsx` | 改（排序/一键接下一/URL筛选/AI按钮移位/右侧默认关/handoff折叠/EmptyState） |
| `src/ServiceWorkspace.jsx` | 改（看板活数据/SlaCountdown/评价增强） |
| `src/AdminConsole.jsx` | 改（import AgentList，people 模块替换） |
| `src/AgentWorkspaceShell.jsx` | 改（状态下拉/手势关闭） |
| `src/AuthPage.jsx` | 改（试用账号引导） |
| `src/polish.css` | 追加样式（一键接下一/AI工具栏按钮/handoff折叠/空状态/评价/presence下拉/AgentList表格/批量栏） |

## 验证

- `npm test`：**40/40 通过**
- `npm run build`：**61 模块编译通过**（新增 3 个组件文件）

## 预览

- 客服工作台：http://localhost:5173/workbench （`lina@xinghe.demo` / `Demo@2026`）
- 客服看板：http://localhost:5173/workbench/dashboard （SLA 倒计时跳动、客服状态切换）
- 注册页试用引导：http://localhost:5173/service/register
- 机构客服列表批量操作：http://localhost:5173/organization/people

## 后续可跟进

- P0-3 继续拆分 `PlatformOverview / OrgOverview / KnowledgeList` 为独立子组件（当前已抽 AgentList）
- P1-8 批量操作接入真实 `POST /api/v1/agents/bulk`
- P0-1 实时数据接入后端 WebSocket / SSE（替换本地 setInterval 模拟）
